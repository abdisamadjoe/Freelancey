"use client";

/**
 * Back-compat surface: pagination now renders through the template-styled
 * primitive. The props contract is unchanged.
 */
export { Pagination } from "@/components/ui/pagination";
export type { PaginationProps } from "@/components/ui/pagination";
