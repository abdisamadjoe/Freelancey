"use client";

/**
 * Back-compat surface: avatars now come from the UI kit so every screen shares
 * the same palette and ring treatment. The props contract is unchanged.
 */
export { Avatar, AvatarGroup } from "@/components/ui/avatar";
