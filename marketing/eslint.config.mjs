import { defineConfig, globalIgnores } from "eslint/config";
import nextVitals from "eslint-config-next/core-web-vitals";
import nextTs from "eslint-config-next/typescript";

const eslintConfig = defineConfig([
  ...nextVitals,
  ...nextTs,
  {
    // The mechanically-converted section markup preserves the original
    // site's exact <img>/style attributes (dimensions, alt text policy,
    // Elementor-managed positioning) rather than switching every image to
    // next/image, since that markup is a deliberate 1:1 port of the
    // captured HTML. See README for the reasoning.
    rules: {
      "@next/next/no-img-element": "off",
    },
  },
  // Override default ignores of eslint-config-next.
  globalIgnores([
    // Default ignores of eslint-config-next:
    ".next/**",
    "out/**",
    "build/**",
    "next-env.d.ts",
  ]),
]);

export default eslintConfig;
